import React from 'react';
import ReactDOM from 'react-dom/client';
import LayoutVariationsPreview from './preview';
import '@/index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <LayoutVariationsPreview />
  </React.StrictMode>,
);
