Atue como um desenvolvedor Frontend Sênior especialista em React, Tailwind CSS, Shadcn UI e Lucide Icons.

Preciso refatorar a tabela da página de "Triagem de Retorno" (/triagem-retorno) para melhorar a densidade de informação e o alinhamento visual. O design deve seguir um padrão moderno, claro e com as informações bem agrupadas, no tema "Titanium" (slate/gray como base).

Por favor, reescreva o componente da Tabela seguindo EXATAMENTE esta estrutura de colunas e estilos:

### Estrutura da Tabela (Shadcn Table)
- Fundo da tabela: `bg-white`, bordas arredondadas, `border border-slate-200`.
- Cabeçalho (TableHeader): Fundo muito sutil `bg-slate-50/50`. Textos em `text-xs font-semibold text-slate-500 uppercase tracking-wider`.
- Linhas (TableRow): `hover:bg-slate-50`. Padding vertical confortável nas células (`py-3` ou `py-4`). Alinhamento vertical centralizado (`align-middle`).

### Detalhamento das Colunas (TableCell):

1. CHECKBOX (Largura mínima)
- Componente `Checkbox` do Shadcn centralizado.

2. MATERIAL / QTD
- Container: `flex items-center gap-3`.
- Esquerda: Um placeholder de imagem (ou miniatura) com `w-10 h-10 rounded bg-slate-100 border border-slate-200 flex items-center justify-center`. Dentro dele, um ícone Lucide `Package` ou a miniatura da arte.
- Direita: `flex flex-col gap-0.5`.
- Linha 1 (Direita): Nome do material (`text-sm font-semibold text-slate-900`) + Badge de quantidade ao lado (`bg-blue-100 text-blue-700 text-[10px] px-1.5 py-0.5 rounded`).
- Linha 2 (Direita): ID do material (`text-xs text-slate-500 font-mono`).

3. EVENTO
- Container: `flex flex-col gap-1`.
- Linha 1: `flex items-center gap-1.5`. Ícone `Calendar` (`w-3.5 h-3.5 text-blue-500`) + Nome do Evento (`text-sm text-slate-700 font-medium`).
- Linha 2: Data (`text-xs text-slate-500 ml-5` para alinhar com o texto de cima).

4. PATROCINADORES
- Container: `flex flex-wrap gap-1.5`.
- Padrão de Chip (Patrocinador ativo): `flex items-center gap-1.5 px-2 py-1 rounded bg-blue-100/50 border border-blue-200 text-xs font-medium text-blue-800`. Com ícone de check `CheckCircle2` (`w-3 h-3 text-green-600`).
- Padrão de Chip (Nenhum Patrocinador): `bg-slate-100 text-slate-600 border border-slate-200 rounded-full px-2.5 py-1 text-xs flex items-center gap-1`.

5. CONDIÇÃO - DESTINO (O Coração da Linha)
- Container: `flex flex-col gap-1.5`.
- Linha 1 (Toggle de Condição): 3 botões em grupo (`flex items-center gap-1`).
  - Botão 1 (Perfeito): Ícone `Sparkles`. Se ativo: `bg-green-100 text-green-700 border border-green-300`. Se inativo: `bg-white border border-slate-200 text-slate-400`.
  - Botão 2 (Avaria): Ícone `Hammer`. Se ativo: `bg-yellow-100 text-yellow-700 border border-yellow-300`.
  - Botão 3 (Sucata): Ícone `Trash2`. Se ativo: `bg-red-100 text-red-700 border border-red-300`.
- Linha 2 (Toggle de Destino): 2 botões em grupo.
  - "GALPÃO": Se ativo: `bg-green-50 text-green-800 border-green-200`.
  - "DESCARTE": Se ativo: `bg-red-50 text-red-800 border-red-200`.
  *(Dica: Pode usar buttons html simples com classes dinâmicas ou o componente ToggleGroup do Shadcn).*

6. OBSERVAÇÃO
- Componente `Input` do Shadcn.
- Estilo: `h-8 text-xs bg-slate-50 border-slate-200 w-full min-w-[150px]`.
- Placeholder: "Adicionar nota...".

7. AÇÃO
- Componente `Button` do Shadcn.
- Tamanho: `size="sm"`.
- Estilo: `bg-slate-900 hover:bg-slate-800 text-white flex items-center gap-1.5`.
- Conteúdo: Ícone `Save` (`w-4 h-4`) + texto "Salvar".

Regras Gerais de UI:
- Substitua informações que estavam aglomeradas (como rótulos e spans) pela estrutura Flexbox acima.
- Certifique-se de usar os ícones corretos do `lucide-react`.
- O código deve ser modular. Se possível, extraia a lógica da coluna "CONDIÇÃO - DESTINO" para um subcomponente chamado `TriageActionToggles`.